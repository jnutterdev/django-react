{
  "name": "django-react",
  "version": "1.0.0",
  "description": "An app created with django and react",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild frontend/src/index.js --bundle --outfile=frontend/static/Bundle.js --loader:.js=jsx --format=cjs",
    "start": "esbuild frontend/src/index.js --bundle --outfile=frontend/static/Bundle.js --loader:.js=jsx --format=cjs --watch"
  },
  "keywords": [
    "django",
    "react",
    "esbuild"
  ],
  "author": "John Nutter",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.19.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.11.0"
  }
}
